{$PUBLIC_HOST} {
  encode zstd gzip
  reverse_proxy app:4317
  header {
    -Server
    Permissions-Policy "camera=(), microphone=(), geolocation=()"
  }
  log {
    output stdout
    format json
  }
}
